<UserControl x:Class="Sonneville.PriceChartTools.Chart"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="300" x:Name="chart">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Viewbox Grid.Column="1" x:Name="viewbox" Width="{Binding Path=Width, ElementName=chart}" Height="{Binding Path=Height, ElementName=chart}">
            <Canvas x:Name="chartCanvas" Background="Blue" Width="{Binding Path=Width, ElementName=chart}" Height="{Binding Path=Height, ElementName=chart}" />
        </Viewbox>
    </Grid>
</UserControl>
